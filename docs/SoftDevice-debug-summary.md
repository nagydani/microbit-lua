# Anchored summary: micro:bit v2 SoftDevice + microbit-lua conflict audit

## Goal
- Reverse-engineer the micro:bit v2 SoftDevice (`/tmp/opencode/sd.bin`) to map nRF52833 IRQ ownership plus stub/gateway/RAM-vector dispatch and PPI/timer usage, then verify the microbit-lua/codal tree never conflicts with SD-owned peripherals/IRQs/PPI channels.
- Now extended: investigate the user hypothesis that the CPP sound/audio code has a memory bug (write to wrong address) that only manifests when BLE (the SoftDevice) is enabled.
- Current phase: root cause found and fixed. The "070" SD asserts were app-priority IRQ work holding PRIMASK (all app IRQs masked) during heap/fiber allocation, which delayed the SD's priority-0 radio-arming IRQs (RTC0/TIMER0/RADIO) past their deadline. Fixes: BASEPRI=1 critical sections in the codal heap + fiber allocators, IMMEDIATE Lua listener, and event filtering. Committed on local branches in the codal dependency repos (below); awaiting board re-test and PR push.

## Constraints & Preferences
- (none explicitly stated)

## Progress
### Done
- **Address-space mapping (verified 3 ways: vector table, rizin `axt` cross-check, byte-level comparison)**: `sd.bin` **file offset = flash − 0x1000**; `full.asm` labels are file offsets; rizin run with `-m 0x1000` prints **flash** addresses. Blob covers flash 0x1000-0x1B400 (file len 0x1A400). Examples: state machine file 0x2E08 = flash 0x3E08; gateway file 0xA058 = flash 0xB058 (stub cookie 0xB059).
- **RE tooling installed**: capstone 5.0.7 (`pip install --break-system-packages capstone`); rizin 0.9.1 static (`/usr/local/bin/rizin` = `bin/rizin` extracted from `rizin-v0.9.1-static-x86_64.tar.xz`; the tarball's multicall `rz-bin` cannot open files). Rizin works: `rizin -q -a arm -b 16 -m 0x1000 -c 'aaa' -c '<cmd>' sd.bin`; `axt`/`pd` work, `pdf` fails on non-function-start, `-i script` mode silent (use `-c`).
- **PPI audit COMPLETE — VERDICT: no conflict.** Verified against official nRF52833 spec (docs.nordicsemi.com PS, PPI "Pre-programmed channels" table):
  - **SD-owned**: dynamic channels 17/18/19 (programmable) + fixed 20-31 (12 pre-programmed channels, individually enable/disable via CHEN only; FORK TEPs app-programmable). Fixed map: ch20 TIMER0 CMP0→RADIO TXEN, ch21 TIMER0 CMP0→RADIO RXEN, ch22 TIMER0 CMP1→RADIO DISABLE, ch23 RADIO BCMATCH→AAR START, ch24 RADIO READY→CCM KSGEN, ch25 RADIO ADDRESS→CCM CRYPT, ch26 RADIO ADDRESS→TIMER0 CAPTURE1, ch27 RADIO END→TIMER0 CAPTURE2, ch28/29 RTC0 CMP0→RADIO TXEN/RXEN, ch30 RTC0 CMP0→TIMER0 CLEAR, **ch31 RTC0 CMP0→TIMER0 START (the timebase)**.
  - **Codal-owned (active in microbit-lua)**: ch0/1 (ADC, NRF52ADC.cpp:455-457,640-642), ch2 (touch, NRF52TouchSensor.cpp:60-62), ch3-7 (LED matrix, NRF52LedMatrix.cpp:99-101, base 3). Codal uses NO channel groups (CHG) and NO FORK. **Sets {0-7} vs {17-31} disjoint.**
  - **SD session-start PPI setup** (file 0x1937c, flash 0x1A37C; callers file 0x122F4/0x1239E/0x123C2): CHENCLR = 0xFFFE0000 (disables ch17-31); zeroes CH17/18/19 EEP+TEP (PPI+0x510/0x514); sanitizes CHG[0-5] at PPI+0x800+4n (mask bits 0-16 for groups 0-3, zero groups 4-5). Never touches ch0-16 EEP/TEP or CHEN bits of ch0-16.
  - **SD session wiring** (file 0x13332-0x133A0): CH17 EEP=RADIO evt (0x40001128), TEP=TIMER0 TASKS_CAPTURE[1] (0x40008044); CH18 EEP=RADIO evt (0x40001110), TEP=TIMER0 TASKS_CAPTURE[0] (0x40008040); CHENSET/CHENCLR masks 0x60000/0x80000/0xE0000 (bits 17-19).
  - **SD fixed-ch22 enable**: CHENSET = 0x400000 (bit 22 = TIMER0 CMP1→RADIO DISABLE) at file 0x13922 and 0x13E0E; CHENCLR=0x80000000 (bit 31 timebase disable) at file 0x962C teardown (clears TIMER0 CC[0], EVENTS_COMPARE[0], COMPARE0 IRQ via INTENCLR 0x308).
  - Note: file 0x13070 `orr r1,#0x1000000` is an **AAR NIRK write** (0x4000F504), NOT a PPI bit-24 write.
  - **PPI dynamic-channel API**: file 0x10F16 (flash 0x11F16) generic helper (CH[ch] EEP/TEP + CHENSET/CHENCLR); file 0x13934 (flash 0x14934) per-slot config (EEP=RADIO EVENTS_ADDRESS 0x40001104, TEP=r1, CHENSET 1<<r0) sole caller file 0x16C96 (flash 0x17C96); guarded enable-time API file 0x192AE/0x192C0/0x192E8 (magic 0xCAFEBABE @ [0x20000058], errors 0x2008/0x2009).
- **Timer audit COMPLETE — VERDICT: no conflict.** SD owns **TIMER0** (IRQ8) exclusively (timebase; TIMER0 CC[0] = us+61 sole writer). Codal uses **TIMER1** (system tick), **TIMER2** (ADC), **TIMER3** (touch), **TIMER4** (LED matrix) — confirmed in libraries/codal-microbit-v2/model/MicroBit.cpp.
- **IRQ-ownership audit (unchanged, stands)**: SD owns IRQ 0 (POWER_CLOCK), 1 (RADIO), 8 (TIMER0), 11 (RTC0), 13 (RNG), 14 (ECB), 15 (CCM_AAR), 25 (TEMP), 32 (SWI). Owned-IRQ stubs at file 0x19F14+6n (cookie pool file 0x19F4C, stride 8): cookies 0x0AC19/0x02FA5/0x0A80B/0x0A799/0x0AEA1/0x01399/0x0774D/0x0B01D/0x1AD2D for IRQs 0,1,8,11,13,14,15,25,32. microbit-lua build touches none of these (map file: RADIO_IRQHandler@0x2B824 / POWER_CLOCK-USBD_IRQHandler@0x37642 linked-but-unreachable).
- **Dead-code/API audit (unchanged)**: microbit-lua never calls MicroBitRadio.enable(), CodalUSB instantiation, or MicroBitPowerManager; `MicroBitDevice.seedRandom` only uses RNG. No NVMC, no SD RAM misuse.
- **Rizin xref results**: gateway flash 0xB058 — no direct callers (indirect-only, expected). Radio state machine flash 0x3E08 (file 0x2E08) — only data ref at flash 0x3C1E (chained-indirect entry). Channel-setter helper flash 0x17CD0 (writes ctx [0x200000F8] +1/+0x20) called from flash 0xF1DC (fn entry flash 0xF1AC).
- **Slot-completion path resolved** (file 0x13DFE / flash 0x14DFE): tiny inline helper that sets slot-ctx flag bit 22 (ctx+0x38, ctx=0x20000A7C) and enables fixed PPI **ch22** (TIMER0 EVENTS_COMPARE[1] → RADIO TASKS_DISABLE). Slot termination = TIMER0 CMP1 → ch22 → RADIO DISABLE, then the TIMER0 ISR (file 0x13E12, flash 0x14E12, IRQ8) reads RADIO EVENTS_END (0x10C)/EVENTS_DISABLED (0x110)/EVENTS_DEVMATCH (0x114)/EVENTS_DEVMISS (0x118)/CRCSTATUS (0x400) to close the slot. This is distinct from the CH17/CH18 programmable wiring (CH17 = RADIO evt→TIMER0 TASKS_CAPTURE[1], CH18 = RADIO evt→TIMER0 TASKS_CAPTURE[0]). Supporting functions: file 0x13D70 (RADIO PHY config: writes PCNF0 = 0x00100108 S0LEN/S1INCL/PLEN-BLE, PCNF1 = 0x02030000 BALEN=3/WHITEEN), file 0x13DAA (reads RADIO CRCSTATUS, sets DACNF/PCNF fields). All within SD-owned IRQ8/slot machinery; no codal interaction.
- **S113 version match COMPLETE — blob is unmodified official S113 v7.0.1**: downloaded official `s113_nrf52_7.0.1_softdevice.hex` (GitHub mirror `tinygo-org/bluetooth`, branch `release/s113_nrf52_7.0.1`; source is Nordic nRF5 SDK 16.0.0 which introduced S113). Converted to binary, compared to `/tmp/opencode/sd.bin` over flash 0x1000-0x1B400 (0x1A400 bytes): **0 byte differences**. MBR (`mbr.bin`, flash 0x0-0xB00) also **0 differences**. Version constants in blob confirmed: flash 0x3010 = 0x71 (variant 113), flash 0x3014 = 0x6ACFC1 (SD_VERSION 7000001). Consequence: micro:bit v2 ships a stock, unmodified SoftDevice; all RE findings (IRQ/PPI/timer ownership) are validated against the reference build, and Nordic's docs apply directly (e.g., RADIO/TIMER0/CCM/AAR only via radio timeslot API; RNG/TEMP restricted via SD APIs).
- **SVC dispatch fully mapped** (SD SVC handler file 0x19EAC / flash 0x1AEAC): reads SVC# from stacked PC; SVC<16 (0x10) → forwarded to app via [0x20000000]+0x2C; SVC 16-31 → handler at flash 0x1B12C; SVC 32-43 → handler at flash 0x1B19B; SVC>=44: error 2 unless magic==0xCAFEBABE@[0x20000058] and SVC>=0x60 → flash 0x137D dispatcher. SDM dispatcher file 0x19586: SVC 16(0x10)→enable 0x19442, 17(0x11)→disable 0x1940A, 18(0x12)→is_enabled 0x1956C, 19(0x13)→vector_table_base_set writes [0x20000004] (matches nrf_sdm.h enum SD_SOFTDEVICE_*).

### Done (this session — RAM layout + sound/BLE investigation)
- **RAM-layout audit COMPLETE — VERDICT: no overlap. The app does NOT overlap the SD.** The app advertises its RAM start to the SD as `APP_RAM_START = &__data_start__ = 0x20002040` (nrf_sdh_ble.c:77-79, GCC branch; `ram_start` flows: MicroBitBLEManager.cpp:264-287 → nrf_sdh_ble_default_cfg_set → sd_ble_cfg_set/sd_ble_enable). S113 `ble.h:394-396` defines the split: **SD RAM = [0x20000000, APP_RAM_BASE-1], app RAM = [APP_RAM_BASE, top of call stack]**.
  - SD RAM: [0x20000000, 0x20002040) (~8 KB, bottom). SD blob vector SP = 0x20000FB0 = the SD's own stack top inside that bottom region (not an app boundary).
  - App RAM: [0x20002040, 0x20020000). App SP/stack at 0x20020000 (`__StackTop` = ORIGIN(RAM)+LENGTH(RAM); DEVICE_STACK_BASE=DEVICE_SRAM_END=0x20020000, 2 KB stack) — correct, inside app RAM.
  - Linker scripts: BLE build uses `ld/nrf52833-softdevice.ld` (RAM 0x20002040-0x20020000); non-BLE uses `ld/nrf52833.ld` (RAM 0x20000010-0x20020000). The project's build-time patch (`source/nrf52833-softdevice.ld.patch`, applied via root CMakeLists.txt:255-284 → `build/nrf52833-patched.ld`) adds ONLY Lua metadata/script FLASH sections — the RAM region is untouched/upstream.
  - `nm` scan of app symbols in [0x20000000, 0x20002040): **exactly one** — `microbit_no_init_memory_region` (16-byte reset-click counter @0x20002030, from MicroBit.cpp:48, section `.noinit`). It sits 16 bytes below `__data_start__` (upstream micro:bit v2 design); triple-reset-to-pair is disabled in this project (MICROBIT_TRIPLE_RESET_TO_PAIR=0) so it is not written.
  - Implication for the "only manifests with BLE" symptom: without BLE the app RAM starts at 0x20000010, so any app write into [0x20000010, 0x20002040) would be harmless memory without BLE but corrupts the SD region with BLE on. That is the ONLY mechanism by which a wrong-address write becomes BLE-only — and no such write exists in the standard pipeline.
- **Sound-pipeline hardware audit COMPLETE — VERDICT: no SD conflict.**
  - `NRF52PWM.cpp`: uses the dedicated PWM0/1/2 peripherals (nRF52833 has 3), DMA **reads** only via SEQ[b].PTR/CNT; **no PPI channels, no TIMER, no channel groups**. IRQs: PWM0/1/2 (IRQ 38/39/40) — NOT in the SD-owned set {0,1,8,11,13,14,15,25,32}; driver installs its own vectors via NVIC_SetVector.
  - `MicroBitAudio.cpp`: output = NRF_PWM1 @ 44.1 kHz, mixer OR-mask 0x8000, speaker (P0.00) on channel 1, edge-connector pin on channel 0; input = NRF52ADC mic channel (PPI ch0/1 — app-owned, already audited).
  - Buffer writes all bounded: `Mixer2.cpp` (`float mix[512]`, loops bounded by CONFIG_MIXER_BUFFER_SIZE/bytesPerSampleOut), `StreamNormalizer.cpp` (per-sample read/write helpers), `SoundEmojiSynthesizer.cpp` (`sample == bufferEnd` guard), `SoundOutputPin.cpp` (bufferWritePos). `ManagedBuffer`-allocated buffers are heap objects in app RAM, far above 0x20002040.
- **CONCLUSION for the user hypothesis ("CPP sound code writes to wrong memory address, only with BLE")**: no supporting evidence. RAM layout is provably disjoint (SD bottom / app top), the sound path touches no SD-owned hardware (PPI 17-31 / TIMER0 / RADIO / CCM / AAR / SD IRQs), and no sound buffer or app data resides in the SD region. A downward buffer underflow into [0x20000010, 0x20002040) would indeed be BLE-only in effect, but no such write was found in the standard codal sound pipeline.

### Done (this session — panic 070 root-cause pivot)
- **New user symptom**: audio is commented out; "vast majority" of power-ups end in sad smiley + **070**, rarely the device boots to the Lua app (which then works normally). Audio is definitively not involved.
- **070 decoded**: `DEVICE_CPU_SDK = 70` (`libraries/codal-core/inc/core/ErrorNo.h:105`); `MICROBIT_PANIC_SD_ASSERT = DEVICE_CPU_SDK` (`MicroBitBLEManager.cpp:1592-1596`). Panic 070 = **SoftDevice internal assertion** (`NRF_FAULT_ID_SD_ASSERT`, id=1, `nrf_sdm.h:177`), not an app memory fault.
- **SD assert mechanism**: SD-internal asserts are `svc 255` (0xff) — **547 sites** in `sd.bin`. SVC handler (file 0x19EAC) → dispatcher (mid-0x137D) → registered fault handler.
- **Fault-handler slot**: pointer stored at RAM **0x20000188** (literal `88 01 00 20` @ file 0x195FC; read helper file 0x195DC). Registered by `sd_softdevice_enable` (file 0x19442), so SD asserts only reach the app handler after enable.
- **Enable/session-start SD sequence mapped** (file 0x1237E): store config ptr → `bl 0x9BC2` (LFCLK driver init; `svc 255` @0x9BC8 if cfg NULL; CLOCK INTENCLR=0xFFFFFFFF @0x40000308; LFCLKSTAT bit31 → calibration 0x124CA) → `bl 0x12214` (clock-config/RTC0-state application driven by flag bits; top suspect for the intermittent boot assert) → `bl 0x12304` → `bl 0x18BFC` → `bl 0x126AE` (CLOCK/LFCLKSRC setup regs base+0x400, +0x41/+0x42) → `bl 0x1937C` (session-start PPI cleanup, audited earlier) → `bl 0x191FE` → `bl 0x9F40` → tail-call 0x18D64. LFCLK start routine file 0x9A66: source switch 0=RC/1=Xtal/2=Synth, else `svc 255` @0x9A94.
- **Clock config handed to SD** (codal.json matches sdk_config.h defaults): `NRF_SDH_CLOCK_LF_SRC=0` (RC), `RC_CTIV=16`, `RC_TEMP_CTIV=2`, `ACCURACY=1`.
- **App fault-handler diagnostics** (`MicroBitBLEManager.cpp:1598-1660`): `app_error_fault_handler(id, pc, info)` → `target_disable_irq()` (polled serial so the dump prints) → `NRF_LOG_FINAL_FLUSH()` + `codal_dmesg_flush()` → `DMESGF("SOFTDEVICE: ASSERTION FAILED pc=%x info=%x")` plus RTC0 (0x4000B000) COUNTER/CC[2]/CC[3]/EVTEN(0x40008514)/PRESCALER, LFCLKSTAT/LFCLKSRC, and SD slot ctx @0x20000A7C — everything needed to interpret the assert state.
- **DMESG serial verified** (this is how we get the pc): `model/MicroBit.cpp:489-502` `microbit_dmesg_flush()` → `serial.putc()` where `serial(io.usbTx, io.usbRx, NRF_UARTE0)` (MicroBit.cpp:73); usbTx = **P0_06**, usbRx = P1_08 (`MicroBitIO.h:279-280`). Default baud 115200. `DEVICE_DMESG_BUFFER_SIZE=1024` guaranteed: codal.json `DMESG_SERIAL_DEBUG=1` → `DMESG_ENABLE=1` (CodalConfig.h:157-168). So the assert dump appears on the micro:bit's USB serial at 115200.
- **Boot order to SD enable**: `uBit.init()` → `bleManager.init()` (`MicroBitBLEManager.cpp:257-268`): `app_timer_init()` → `nrf_sdh_soc_init()` → `nrf_pwr_mgmt_init()` → `nrf_sdh_enable_request()` (nrf_sdh.c:181-235) → `sd_softdevice_enable(&clock_lf_cfg, app_error_fault_handler)`.
- **Pre-BLE RTC0/LFCLK usage scan**: `source/` — zero references; libraries — only nrfx templates (`NRFX_RTC0_ENABLED=0` etc.) and SDK `drv_rtc.c:345-352` which aliases `RTC0_IRQHandler` **only** under `APP_TIMER_V2_RTC0_ENABLED` (flag value **not yet verified**). If app_timer is RTC0-based, the app installs RTC0_IRQHandler before SD enable → intermittent SD assert; official codal builds use RTC1.
- **IRQ-number table re-verified** against the repo's `nrf52833.h:84-125`: 0 POWER_CLOCK, 1 RADIO, 8 TIMER0, 11 RTC0, 13 RNG, 14 ECB, 15 CCM_AAR, 25 SWI5_EGU5, 32 MWU/SWI — confirms the SD-owned IRQ audit {0,1,8,11,13,14,15,25,32} and the SD enable-time IRQ-priority setup (file 0x30C: RADIO/RTC0/TIMER0 prio 0, SWI5/EGU4 prio 4, etc.).
- **Audio-bug hypothesis archived**: audio commented out + panic is an SD-internal assert → the CPP-sound-memory-bug theory is no longer the primary lead.

### Done (this session — 070 root cause found & fixed; upstream commits prepared)
- **Root-cause chain (4 stages, each A/B-verified or instrumented)**:
  1. **Boot-time 070**: the Lua compile (`luaL_newstate`/`luaopen_*`/`register_lua_api`/`luaL_loadbuffer`) ran *after* `uBit.init()` began BLE start, causing heavy heap churn while the SD was arming its radio. Fix: compile Lua before `uBit.init()` in `source/main.cpp`.
  2. **Idle-phase 070 (tick flood)**: `Event(DEVICE_ID_SCHEDULER, DEVICE_ID_COMPONENT_RUNNING)` and friends fired ~166x/s and each spawned a Lua fiber. Fix: filter `DEVICE_ID_SCHEDULER` (15)/`DEVICE_ID_COMPONENT` (16) in `on_codal_event`.
  3. **Audio-triggered 070**: `Mixer2` does `ManagedBuffer` allocations inside the PWM1 ISR, under a full PRIMASK window (`target_disable_irq()`), delaying the SD radio-arming IRQs. Fix: BASEPRI heap allocator (below). User-verified "no crash now!" with audio re-enabled.
  4. **REPL-banner 070 (the last PRIMASK window)**: every serial TX char fires `Event(DEVICE_ID_NOTIFY, CODAL_SERIAL_EVT_TX_EMPTY)` from the UARTE IRQ (Serial.cpp:75); with the IMMEDIATE listener this reached `create_fiber()` in IRQ context, and `getFiberContext()` masked PRIMASK across `new Fiber` + `tcb_allocate` (a malloc). All 4 captured logs died at the same spot — printing `micro:bit` / `Lua 5.1 REPL` (hb=13, rtc≈69000, `+44`=440–2131). Fix: BASEPRI in `getFiberContext` + filter `DEVICE_ID_NOTIFY` (1023)/`DEVICE_ID_NOTIFY_ONE` (1022).
- **BASEPRI allocator fixes (codal-core)**: `CodalHeapAllocator.cpp` (`device_malloc_in`/`device_create_heap`) and `CodalFiber.cpp` (`getFiberContext`) use inline-asm `MRS basepri`/`MSR basepri,1` critical sections. BASEPRI=1 blocks app-priority IRQs (1–7) but leaves the SD's priority-0 IRQs (RTC0/TIMER0/RADIO) live — nothing below priority 0 touches the heap/fiber queues, so preemption is benign. This eliminates the whole class "any slow heap walk can blow the ~17–36 µs radio arming deadline".
- **IMMEDIATE listener is required**: switching to `MESSAGE_BUS_LISTENER_NORMAL`/`QUEUE_IF_BUSY` made serial head-match events never reach Lua (NORMAL listeners drain only via `DEVICE_SCHEDULER_EVT_IDLE` → `MessageBus::idle` → `process()`). Reverted to `MESSAGE_BUS_LISTENER_IMMEDIATE`; safe now that dispatch-path allocations run under BASEPRI.
- **Panic 030 also observed**: `DEVICE_HEAP_ERROR` (heap corruption; panic sites `RefCounted.cpp:52`, `CodalHeapAllocator.cpp:317/388/395`) occurred at the same REPL-banner point. Distinct class from 070 (SD assert); whether it survives the final build is still open.
- **Supporting robustness fixes**:
  - codal-nrf52 `NRF52Serial.cpp`: `putc()` TX watchdog (force-STOPTX after ~300 ms if the UARTE IRQ never clears `is_tx_in_progress_`, so a wedged DMA can't hang the system) + polled-path STOPTX-before-reprogram so the polled path is valid at IRQ priority (fault-handler printing).
  - codal-microbit-v2 `MicroBitBLEManager.cpp` fault handler: `target_disable_irq()` + `codal_dmesg_flush()` so the full dump prints, and `DMESGF("SOFTDEVICE: ... pc=%x info=%x")` so the assert site is reported.
- **Local-only diagnostics** (kept in working trees, NOT in upstream commits): `MicroBit::idleCallback` heartbeat `DMESG("hb=%u txwd=%u rtc=%u")`, `g_nrf52serial_tx_watchdog_count`, the fault handler's raw `rtc:`/`lfc:`/`sd:` register dumps, BLE-init `MICROBIT_DEBUG_DMESGF` "done" traces, the IRQ-priority comment edit, and the codal.json debug flags.
- **Upstream commits prepared (local branches; nothing pushed yet)**:
  - codal-core `fix/basepri-allocators` (off 3d485ab): `9022fa9` (heap BASEPRI), `376c251` (fiber BASEPRI).
  - codal-nrf52 `fix/serial-tx-watchdog` (off 140d1be): `33e0cfd` (polled-path STOPTX fix), `dd23d59` (bounded TX spin-waits / TX watchdog; counter stripped from commits, restored in working tree).
  - codal-microbit-v2 `fix/sd-fault-handler` (off upstream `b907e6a`): `b5170fd` (namespace fix → PR A) + `8765e49` (fault handler pc/info + flush; rtc/lfc/sd dumps + init traces stripped).
  - Main repo: no commits by policy; only the dependency repos carry upstream-bound commits. `python3 build.py --clean` still builds with all local-only diagnostics restored.
- **Audio-bug hypothesis fully archived**: the sound pipeline was audited clean (no SD hardware/PPI/RAM overlap) and the actual root cause was IRQ-priority/PRIMASK vs the SD radio deadline, not a memory bug.

### In Progress
- Final build (BASEPRI fiber + NOTIFY/NOTIFY_ONE filter + IMMEDIATE listener; audio on, startup scroll commented) builds clean but has not yet been flashed/tested on the board.
- Whether panic 030 (heap corruption) survives the final build is unverified.
- Pushing/opening the prepared codal PRs (needs a fork remote or direct-push permission on the three repos).

### Blocked
- PR push target: the three codal repos have only `origin` (lancaster-university) remotes; no fork remote configured yet. Branches/commits are ready locally (see Done).
- Final board re-test of the current build is outstanding (unflashed).

## Key Decisions
- Address-space rule: objdump/`full.asm` = file offsets; flash = file + 0x1000; rizin `-m 0x1000` = flash.
- PPI verdict: no conflict — SD channels 17-31 vs codal 0-7 disjoint; SD programs PPI only during radio sessions (never in microbit-lua, which has no BLE radio activity); codal never calls SD PPI SVCs.
- Timer verdict: no conflict — SD TIMER0 vs codal TIMER1-4 disjoint.
- IRQ verdict: no conflict in current build; two latent hazards if features are added later: instantiating `CodalUSB`/`NRF52USB` and `MicroBitRadio.enable()`/`powerOff()` while BLE is on.
- RE approach: rizin + capstone + objdump + python pool decode (chosen over Ghidra headless: smaller install, targeted needs). S113 version match now complete (stock v7.0.1).
- Sound/BLE hypothesis: judged via the SD memory model documented in S113 `ble.h` + the build's actual `APP_RAM_START` (0x20002040). The hypothesis as stated (sound code overwrites SD RAM / SD-owned hardware) is not supported; if a real BLE+sound bug exists, obtain the symptom or a HardFault PC/LR before further digging.
- **Pivot (this session, historical)**: primary hypothesis became an **SD-internal boot-time assert** of stock S113 v7.0.1 triggered by app or board state at enable time — not an app memory/audio bug (audio was commented out at that point; 070 = SD assert).
- Suspects ranked (historical — superseded by the BASEPRI/PRIMASK root cause in the Done section): (1) clock-config/RTC0-state asserts in SD file 0x12214 during enable; (2) LFCLK RC-start race (config uses RC source, CTIV 16/2); (3) `app_timer_init()` on a timer colliding with SD's RTC0 — check `APP_TIMER_V2_RTC0_ENABLED`.
- Do NOT patch the SD; read the DMESG `pc` to locate the assert site first.
- **BASEPRI vs PRIMASK**: replace full-PRIMASK critical sections (`target_disable_irq`/`__disable_irq`) around heap + fiber allocation with BASEPRI=1. App IRQs (1–7) stay excluded → heap/queue integrity preserved; SD priority-0 IRQs stay live → radio arming deadline never delayed. Short PRIMASK windows in `queue_fiber`/`dequeue_fiber` are kept (no allocations inside — safe).
- **IMMEDIATE listener is mandatory** for the Lua bridge: NORMAL/`QUEUE_IF_BUSY` relied on scheduler-idle-driven draining and lost serial events; IMMEDIATE is safe because dispatch-path allocations are now BASEPRI-guarded.
- **Event filtering at `on_codal_event`**: block `DEVICE_ID_SCHEDULER`/`DEVICE_ID_COMPONENT` (tick flood) and `DEVICE_ID_NOTIFY`/`DEVICE_ID_NOTIFY_ONE` (per-char TX_EMPTY fiber spawns).
- **Main repo: no commits.** Upstream-bound changes live only in the codal dependency repos; main-repo changes (codal.json, main.cpp, codal-lua.cpp, lua-script.lua) stay uncommitted.
- **Sanitize upstream PRs**: strip local-only diagnostics (heartbeat, watchdog counter, raw register dumps, init traces, debug flags) from committed content; restore them in the working trees so local builds keep the diagnostics.

## Next Steps
1. **Flash the current `build/MICROBIT.hex`** (BASEPRI fiber + NOTIFY/NOTIFY_ONE filter + IMMEDIATE listener; audio on, scroll commented). Verify REPL banner/echo serial traffic causes no 070, and loop-boot to check for 030.
2. If 030 (heap corruption) persists: instrument `device_free`/`RefCounted::isReadOnly` and audit `ManagedBuffer`/Mixer2 lifetimes (double-free / overrun).
3. Push the prepared branches and open the PRs (needs a fork remote on the three codal repos): codal-core `fix/basepri-allocators`, codal-nrf52 `fix/serial-tx-watchdog`, codal-microbit-v2 `b5170fd` + `fix/sd-fault-handler`.
4. Cleanup: delete `source/main.cpp.lua-bak` and `working-070-panic.patch` (superseded).

## Critical Context
- **Address space**: `sd.bin` file offset = flash − 0x1000; `full.asm` labels = file; rizin with `-m 0x1000` = flash. Blob = flash 0x1000-0x1B400.
- **RAM split (this session)**: SD RAM = [0x20000000, 0x20002040) (bottom ~8 KB), app RAM = [0x20002040, 0x20020000) (top). App SP 0x20020000. SD vector SP 0x20000FB0. APP_RAM_START mechanism: `nrf_sdh_ble.c` GCC branch → `&__data_start__` (0x20002040) → passed via MicroBitBLEManager ram_start → sd_ble_cfg_set/sd_ble_enable. Only app symbol in SD region: `microbit_no_init_memory_region` @0x20002030 (unused in this project). Non-BLE build: RAM 0x20000010-0x20020000.
- **Sound hardware (this session)**: NRF52PWM uses PWM0/1/2 + DMA reads only; no PPI/TIMER; PWM IRQs 38/39/40 not SD-owned. Pipeline: MicroBitAudio → NRF_PWM1 ← Mixer2 ← SoundEmojiSynthesizer (+SampleSource for mic), StreamNormalizer/StreamSplitter for input. All buffer writes bounded; buffers heap-allocated in app RAM.
- **PPI final map**: SD dynamic ch17/18/19; fixed ch20-31 (spec-confirmed; ch31 = timebase RTC0 CMP0→TIMER0 START; ch22 = TIMER0 CMP1→RADIO DISABLE; ch25 = RADIO ADDRESS→CCM CRYPT; ch26/27 = RADIO→TIMER0 CAPTURE1/2; ch24 = RADIO READY→CCM KSGEN; ch23 = RADIO BCMATCH→AAR START; ch28/29 = RTC0→RADIO TXEN/RXEN; ch30 = RTC0→TIMER0 CLEAR). Session-start cleanup: CHENCLR ch17-31, zero CH17-19, sanitize CHG[0-5]. Codal: ch0/1 ADC, ch2 touch, ch3-7 LED (no CHG/FORK).
- **Timers final map**: SD TIMER0 (IRQ8); codal TIMER1 (tick), TIMER2 (ADC), TIMER3 (touch), TIMER4 (display).
- **Gateway flow (file offsets)**: file 0xA058 (=flash 0xB058) magic check → `bx r3`; 0xA064 → lock dance 0x12888/0x12E32 → 0xA07C RAMVT via [0x20000004]; 0xA044 direct trampoline. MBR (flash 0x0-0x1000): stubs 0x714+ = single indirection [0x20000000]+off; launch 0xA84 sets [0x20000000]; SVC fwd 0xAA4; HardFault fwd 0xA60.
- **SD vector table** (file 0x40-0xE4): SP=0x20000FB0, reset 0x1AE21; fault/SysTick → 0xB065; HardFault → 0x1AD87; SVC → 0x1AEAD.
- **SVC table** (dispatcher file 0x19586): 16 → file 0x19442 (sd_softdevice_enable), 17 → file 0x1940a (disable), 18 → file 0x1956c, 19 → file 0x1957e (writes [0x20000004] = vector table base set). SVC <16 forwarded via [0x20000000]+0x2C; SVC ≥0x60 (with magic set) → file 0x137D dispatcher; HardFault forward via [0x20000004]+[+12].
- **Scheduler/slot RAM (file offsets in earlier notes)**: base 0x20000958, state table 0x20000E6C (16 B/entry), slot ctx 0x20000A7C (+38 flags, +44 TIMER0 CC[0]), BLOCK_CLOCK 0x20000A54, flash-session 0x20000A18, timer-struct 0x20000A20, pending 0x20000A60, SD RAM 0x20000FB0, magic 0x20000058, ctx 0x200000F8 (channel setter helper writes [+1]/[+0x20]).
- Codal config: DEVICE_BLE=1, MICROBIT_BLE_ENABLED=1, NRF_SDH_CLOCK_LF_SRC=0 (RC) in codal.json — SD active, RC LFCLK.
- **Panic mapping (this session)**: 070 = DEVICE_CPU_SDK = MICROBIT_PANIC_SD_ASSERT = NRF_FAULT_ID_SD_ASSERT (id=1) = an SD-internal `svc 255` assert. Fault handler ptr @ RAM 0x20000188 (literal file 0x195FC `88 01 00 20`, read helper 0x195DC), registered by sd_softdevice_enable.
- **Boot order to SD enable**: uBit.init() → bleManager.init(): app_timer_init() (MicroBitBLEManager.cpp:257) → nrf_sdh_soc_init() → nrf_pwr_mgmt_init() → nrf_sdh_enable_request() → sd_softdevice_enable(&clock_lf_cfg, app_error_fault_handler).
- **SD enable-time assert candidates**: file 0x1237E (enable/session-start init: LFCLK init 0x9BC2 `svc 255`@0x9BC8 NULL-cfg; clock-config apply 0x12214; CLOCK/LFCLKSRC 0x126AE; PPI cleanup 0x1937C); LFCLK start 0x9A66 `svc 255`@0x9A94 invalid source. Clock cfg: RC, CTIV=16, temp CTIV=2, accuracy=1.
- **DMESG serial path**: DMESG buffer (DEVICE_DMESG_BUFFER_SIZE=1024 via DMESG_SERIAL_DEBUG=1) flushed by microbit_dmesg_flush() → serial.putc() (UARTE0, usbTx=P0_06, usbRx=P1_08, 115200).
- **070 vs 030**: 070 = `MICROBIT_PANIC_SD_ASSERT` (SD-internal `svc 255` assert; radio-arming deadline overrun, pc=0x146E4 in all captured logs); 030 = `DEVICE_HEAP_ERROR` (heap corruption; panic sites `RefCounted.cpp:52`, `CodalHeapAllocator.cpp:317/388/395`).
- **SD IRQ priorities**: RADIO/TIMER0/RTC0 = priority 0 after SD enable; assert margin ≈17–36 µs. Only full PRIMASK could delay them; BASEPRI=1 leaves them live.
- **Last 070 window**: `getFiberContext()` (CodalFiber.cpp:130-174) masked PRIMASK across `new Fiber` + `tcb_allocate`; per-char `DEVICE_ID_NOTIFY`/`CODAL_SERIAL_EVT_TX_EMPTY` events from the UARTE IRQ spawned fibers in IRQ context during the REPL banner print.
- **Listener/ID constants**: `DEVICE_ID_SCHEDULER=15`, `DEVICE_ID_COMPONENT=16`, `DEVICE_ID_NOTIFY=1023`, `DEVICE_ID_NOTIFY_ONE=1022`; `EVENT_LISTENER_DEFAULT_FLAGS = MESSAGE_BUS_LISTENER_QUEUE_IF_BUSY`.
- **Heartbeat (local-only)**: `MicroBit::idleCallback` `DMESG("hb=%u txwd=%u rtc=%u")` — hb=13 at every captured crash.

## Relevant Files
- `/tmp/opencode/s113_701.hex`, `/tmp/opencode/s113_701.bin`: official S113 v7.0.1 SoftDevice (reference; sd.bin is byte-identical over flash 0x1000-0x1B400, MBR identical over 0x0-0xB00) — from `github.com/tinygo-org/bluetooth`, branch `release/s113_nrf52_7.0.1`
- `/tmp/opencode/sd.bin`, `/tmp/opencode/full.asm`: SD image/disassembly — file offsets = flash − 0x1000
- `/tmp/opencode/mbr.bin`, `/tmp/opencode/mbr.asm`: MBR image/disassembly
- `/root/microbit-lua/build/MICROBIT.map`: app RAM layout — RAM 0x20002040-0x20020000, `.noinit` @0x20002030, `__data_start__` = 0x20002040, RADIO_IRQHandler@0x2B824 / POWER_CLOCK-USBD_IRQHandler@0x37642 linked-but-unreachable
- `/root/microbit-lua/libraries/codal-microbit-v2/ld/nrf52833-softdevice.ld` vs `ld/nrf52833.ld`: BLE (RAM 0x20002040) vs non-BLE (RAM 0x20000010); `/root/microbit-lua/source/nrf52833-softdevice.ld.patch` (+ root CMakeLists.txt:255-284): only adds Lua FLASH sections
- `/root/microbit-lua/libraries/codal-microbit-nrf5sdk/nRF5SDK/components/softdevice/common/nrf_sdh_ble.c:68-84`: APP_RAM_START = `&__data_start__` (GCC); `nrf_sdh.c:181-235`: sd_softdevice_enable flow; `.../s113/headers/ble.h:386-414`: SD RAM [0x20000000, APP_RAM_BASE-1] contract
- `/root/microbit-lua/libraries/codal-microbit-v2/source/bluetooth/MicroBitBLEManager.cpp:264-287`: `ram_start=0` → nrf_sdh_ble_default_cfg_set → sd_ble_cfg_set
- Sound pipeline: `/root/microbit-lua/libraries/codal-nrf52/source/NRF52PWM.cpp`, `/root/microbit-lua/libraries/codal-microbit-v2/source/{MicroBitAudio.cpp,Mixer2.cpp,SoundEmojiSynthesizer.cpp,SoundOutputPin.cpp}`, `/root/microbit-lua/libraries/codal-core/source/streams/StreamNormalizer.cpp`, `inc/Mixer2.h` (CONFIG_MIXER_BUFFER_SIZE 512, `float mix[512]`)
- `/root/microbit-lua/libraries/codal-microbit-v2/inc/MicroBitMemoryMap.h` + `model/MicroBit.cpp:48`: `.noinit` reset-counter region @0x20002030
- `/root/microbit-lua/libraries/codal-nrf52/nrfx/mdk/nrf52833.h`: PPI base 0x4001F000, TIMER0 @0x40008000
- `/root/microbit-lua/codal.json`: DEVICE_BLE=1, MICROBIT_BLE_ENABLED=1, NRF_SDH_CLOCK_LF_SRC=0 (RC), DMESG_SERIAL_DEBUG=1, CODAL_DEBUG=1, MICROBIT_DMESG_LEVEL=4, MICROBIT_TRIPLE_RESET_TO_PAIR=0
- Panic wiring: `/root/microbit-lua/libraries/codal-core/inc/core/ErrorNo.h:105` (DEVICE_CPU_SDK=70); `/root/microbit-lua/libraries/codal-microbit-v2/source/bluetooth/MicroBitBLEManager.cpp:1592-1660` (MICROBIT_PANIC_SD_ASSERT, app_error_fault_handler DMESG dumps RTC0 0x4000B000 / LFCLK 0x40000000 / SD ctx 0x20000A7C); `model/MicroBit.cpp:73,489-502` (serial UARTE0 P0_06, dmesg flush); `model/MicroBitIO.h:279-280` (usbTx P0_06, usbRx P1_08)
- SD enable path: `/root/microbit-lua/libraries/codal-microbit-nrf5sdk/nRF5SDK/components/softdevice/common/nrf_sdh.c:181-235` (nrf_sdh_enable_request → sd_softdevice_enable); `.../s113/headers/nrf_sdm.h:171-178,287` (NRF_FAULT_ID_SD_ASSERT=1); `sdk_config.h:5044-5083` (NRF_SDH_CLOCK_LF_* defaults); `nRF5SDK_mods/drv_rtc.c:345-352` (RTC0_IRQHandler alias under APP_TIMER_V2_RTC0_ENABLED — unverified)
- SD disassembly: `/tmp/opencode/full.asm` + `/tmp/opencode/sd.bin` — svc-255 assert sites (547), enable/session-start file 0x1237E, LFCLK init 0x9BC2, LFCLK start 0x9A66, clock-config apply 0x12214, CLOCK setup 0x126AE, fault-handler literal 0x195FC (→0x20000188) / read helper 0x195DC
- Web: nRF52833 PS PPI fixed-channel table (docs.nordicsemi.com/r/bundle/ps_nrf52833/page/ppi.html)
- **BASEPRI fixes (codal-core, committed)**: `libraries/codal-core/source/core/CodalHeapAllocator.cpp` (`heap_lock_irq`/`heap_unlock_irq`), `.../CodalFiber.cpp` (`fiber_lock_irq`/`fiber_unlock_irq`, `getFiberContext`) — branches `fix/basepri-allocators` @ 9022fa9/376c251
- **Serial fixes (codal-nrf52, committed)**: `libraries/codal-nrf52/source/NRF52Serial.cpp` — `33e0cfd` polled-path STOPTX + `dd23d59` bounded TX spin-waits/watchdog; `g_nrf52serial_tx_watchdog_count` restored local-only. Branch `fix/serial-tx-watchdog`
- **Fault-handler fix (codal-v2, committed)**: `libraries/codal-microbit-v2/source/bluetooth/MicroBitBLEManager.cpp` — pc/info + `codal_dmesg_flush()` upstream. Branch `fix/sd-fault-handler` @ 8765e49 (+ `b5170fd` namespace fix, `MicroBitBLETypes.cpp`)
- **App-side (main repo, uncommitted)**: `source/main.cpp` (compile-before-init), `source/codal-lua.cpp` (`on_codal_event` filters + IMMEDIATE listener), `source/lua-script.lua` (audio on, scroll commented)
- **030 panic sites**: `libraries/codal-core/source/types/RefCounted.cpp:52`, `.../core/CodalHeapAllocator.cpp:317/388/395`
